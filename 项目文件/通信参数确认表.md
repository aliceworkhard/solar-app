# 通信参数确认表（安卓阶段）

更新时间：2026-04-22

## 1. 设备识别信息

| 项目 | 值 | 状态 | 备注 |
| --- | --- | --- | --- |
| 设备名称前缀 | `AC632N` | 已确认 | 扫描过滤用 |
| 设备名称示例 | `AC632N_1` | 已确认 | 来自调试结果 |
| 是否依赖 MAC 地址 | 否 | 已确认 | 仅做参考，不做主识别 |

## 2. GATT 映射信息

| 项目 | 值 | 状态 | 备注 |
| --- | --- | --- | --- |
| Service UUID | `0000FFF0-0000-1000-8000-00805F9B34FB` | 已确认 | 业务服务 |
| Write UUID | `0000FFF1-0000-1000-8000-00805F9B34FB` | 已确认 | 发送通道 |
| Notify UUID | `0000FFF2-0000-1000-8000-00805F9B34FB` | 已确认 | 接收通道（你已修正） |
| 写入方式 | `WRITE / WRITE_NO_RESPONSE` | 待确认 | 读取 `FFF1` 属性后定 |
| MTU | `20`（初始） | 待确认 | 先按 20 字节分包处理 |

## 3. 连接与权限参数

| 项目 | 值 | 状态 | 备注 |
| --- | --- | --- | --- |
| 是否需要配对 | 待确认 | 待确认 | 连接流程中验证 |
| 是否需要握手命令 | 待确认 | 待确认 | 按协议实测 |
| 连接超时阈值 | `10s`（建议） | 待确认 | 可按机型调整 |
| 命令超时阈值 | `2s`（建议） | 待确认 | 与重发策略联动 |
| 重发次数 | `2`（建议） | 待确认 | 仅对可重试命令启用 |

## 4. 属性确认记录（必须补齐）

| 日期 | 手机型号 | 系统版本 | `FFF1` 属性 | 结论写入方式 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 2026-04-22 |  |  |  |  |  |

## 5. 结论口径（当前）

- 当前使用参数：
  - `serviceUUID = FFF0`
  - `writeUUID = FFF1`
  - `notifyUUID = FFF2`
- 当前唯一关键未决项：`writeType`。
- 在 `writeType` 未确认前，调试页需支持两种写法切换测试。
