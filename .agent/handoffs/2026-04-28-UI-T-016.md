# Handoff - UI T-016

## Role

UI Agent

## Scope Completed

- Realigned the two-page MVP UI toward the provided reference images and `HTML参考/app.html`.
- Updated the home page visual structure:
  - reference-style title/subtitle,
  - plus scan button,
  - scan status card,
  - nearby-device card,
  - target-device status overview.
- Updated the control page visual structure:
  - centered top title and status,
  - segmented tabs,
  - device detail card,
  - telemetry grid,
  - mode selector,
  - control panel with five MVP commands.
- Added `REFERENCE_UI_COPY` and a UI copy regression test.
- Captured new 390px mobile screenshots.

## Preserved Boundaries

- Did not modify BLE bridge files.
- Did not modify protocol files.
- Did not modify `项目文件/android-mvp-capacitor/src/device/deviceController.ts`.
- Did not modify Android native plugin files.
- Did not add scene/profile/OTA/advanced setting pages.
- Did not add Tailwind, React, icon libraries, or remote font dependencies.

## Preserved Behavior

- Only `AC632N_1` enters the UI list/connect flow.
- Scan discovery can auto-connect the target.
- Connect success still sends one `readStatus`.
- Tapping an already connected target device still opens the control page instead of reconnecting.
- Hidden debug console remains available through the existing hidden tap flow.
- UI still calls only existing `DeviceController` methods and does not parse raw HEX.

## Verification

- RED check: `npm.cmd test -- src/app.test.ts` failed before implementation because `REFERENCE_UI_COPY` was undefined.
- `npm.cmd test -- src/app.test.ts`: passed, 1 file / 11 tests.
- `npm.cmd test`: passed, 5 files / 32 tests.
- `npm.cmd run build`: passed.
- Chrome CDP mobile check, 390px width:
  - Home: `scrollWidth=390`.
  - Control: `scrollWidth=390`.

## Evidence Files

- `.agent/reports/screenshots/T-016-home-mobile-390x900-cdp.png`
- `.agent/reports/screenshots/T-016-control-mobile-390x900-cdp.png`

## Notes For Next Agent

- The screenshots are browser/CDP states; real phone visual review with connected BLE device is still recommended.
- The control-page screenshot was forced through DOM class switching for visual layout only; real app navigation still requires `ready` state.
- `项目文件/android-mvp-capacitor/android/.idea/` remains untracked and must not be included in Git unless the user explicitly asks.
