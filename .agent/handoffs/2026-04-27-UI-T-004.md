# Handoff 2026-04-27 UI T-004

## Role

UI Agent

## Task

- `.agent/tasks/done/T-004-ui-convergence.md`（本轮开始时来自 `.agent/tasks/active/`）

## Summary

- 首页保留扫描、快连、断开、设备列表、连接阶段和关键状态。
- 新增全局反馈条，首页操作结果不再只写到控制页隐藏区域。
- 控制页命令按钮由 `BUSINESS_COMMANDS` 统一渲染，当前映射为：
  - `readStatusBtn` -> `DeviceController.readStatus`
  - `readParamsBtn` -> `DeviceController.readParams`
  - `powerToggleBtn` -> `DeviceController.powerToggle`
  - `brightnessUpBtn` -> `DeviceController.brightnessUp`
  - `brightnessDownBtn` -> `DeviceController.brightnessDown`
- 控制按钮只在 `connectionState === "ready"` 后启用。
- 调试台隐藏保留：显式调试按钮移除，品牌区域 5 次点击仍可打开。

## Changed Files

- `项目文件/android-mvp-capacitor/src/app.ts`
- `项目文件/android-mvp-capacitor/src/app.test.ts`
- `项目文件/android-mvp-capacitor/src/styles.css`
- `todo.md`
- `.agent/AI_CONTEXT.md`
- `.agent/logs/2026-04-27-session-04.md`

## Verification

- Red：`npm.cmd test -- src/app.test.ts` 先失败，失败原因为 UI helper 尚未实现。
- Green：实现后同一命令通过。
- `npm.cmd test`：5 个测试文件、20 个用例通过。
- `npm.cmd run build`：TypeScript 与 Vite 构建通过。

## Next Agent Notes

- UI 不直接解析 HEX；后续协议变动只调整 `BUSINESS_COMMANDS` 或 `DeviceController` 暴露方法。
- 若协议确认独立开机/关机，需要由 Protocol/DeviceController 先暴露明确方法，再让 UI 改按钮。
