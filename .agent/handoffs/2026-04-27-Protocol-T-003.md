# Handoff 2026-04-27 Protocol T-003

## Role

Protocol Agent + Orchestrator

## Task

- `.agent/tasks/done/T-003-response-policy.md`（本轮开始时来自 `.agent/tasks/active/`）

## Summary

- 5 条 MVP 命令继续默认不等待回包。
- `DeviceController` 对显式 `waitForResponse=true` 的命令仍支持等待 notify。
- 等待命令如果写入失败，现在会清理对应 `pendingResponse`，避免 retry 立刻报 `Command conflict`。

## Changed Files

- `项目文件/android-mvp-capacitor/src/device/deviceController.ts`
- `项目文件/android-mvp-capacitor/src/device/deviceController.test.ts`
- `项目文件/最小命令集表.md`
- `todo.md`
- `.agent/AI_CONTEXT.md`
- `.agent/logs/2026-04-27-session-03.md`

## Verification

- Red：`npm.cmd test -- src/device/deviceController.test.ts -t "cleans pending response"` 先失败，失败原因为 pending 未清理导致 `Command conflict`。
- Green：修复后同一命令通过。
- `npm.cmd test`：4 个测试文件、18 个用例通过。
- `npm.cmd run build`：TypeScript 与 Vite 构建通过。

## Next Agent Notes

- 优先继续 T-001 与 T-002，补齐真机性能采样和 5 条 MVP 命令复测。
- 不要把普通命令重新改成默认等待回包。
- 只有真机复测确认稳定 notify 的特定命令，才应在 `CommandDefinition` 中开启 `waitForResponse=true`。
