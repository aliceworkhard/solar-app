# Session Log 2026-04-27 Session 08

## 目标

- 执行 T-009 当前阶段：持续发现、5 秒补扫、同名设备按 MAC 区分、已连接设备不清除。

## 变化

- App 扫描按钮改为开始/停止持续发现。
- 启动持续发现后立即扫描一轮，之后每 5 秒调度下一轮。
- 设备列表改为按 `deviceId/MAC` 唯一维护，同名设备同时显示。
- 新扫描结果会追加/更新列表，不再每次清空。
- 未再次出现的设备先标记“最近见过”，超过保留窗口且未连接才移出。
- 已连接设备置顶并标记“已连接”，后台补扫不清除。
- 进入控制页会暂停持续发现，避免控制操作时抢 BLE。
- `DeviceController.scan()` 在已连接 `ready` 状态下进行后台扫描后会恢复 `ready`，避免控制按钮被打回 `idle`。

## 文件

- `项目文件/android-mvp-capacitor/src/app.ts`
- `项目文件/android-mvp-capacitor/src/app.test.ts`
- `项目文件/android-mvp-capacitor/src/styles.css`
- `项目文件/android-mvp-capacitor/src/device/deviceController.ts`
- `项目文件/android-mvp-capacitor/src/device/deviceController.test.ts`
- `todo.md`
- `.agent/AI_CONTEXT.md`

## 验证

- Red：`npm.cmd test -- src/app.test.ts` 先失败，原因是持续发现 helper 尚未实现。
- Red：`npm.cmd test -- src/device/deviceController.test.ts -t "background scan"` 先失败，原因是后台扫描后状态变为 `idle`。
- Green：上述两个局部测试修复后通过。
- `npm.cmd test`：5 个测试文件、25 个用例通过。
- `npm.cmd run build`：TypeScript 与 Vite 构建通过。

## 后续

- T-010：根据真机数据优化补扫间隔、过期窗口、进入控制页后的低频保活策略。

## 风险

- 本轮未做 Android 真机扫描验证。
