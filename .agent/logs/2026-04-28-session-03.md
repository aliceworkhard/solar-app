# Session Log - 2026-04-28 session 03

## Task

T-018 Android back dispatch and Live Status layout.

## User Feedback Addressed

- T-017 fixed homepage refresh, but vivo/Android right-edge system back still exited the app from the control page.
- The read-status feedback area needed to follow the provided `LIVE STATUS` screenshot and `HTML参考/app.html` structure.
- Nearby-device cards needed a 2x2 status block: current mode, battery voltage, solar voltage, brightness.

## Root Cause

- T-017 only used browser/WebView history (`history.pushState` / `popstate`). On the tested phone, Android system gesture back was handled by the native Activity path and exited before the web history handler could route the app back to the homepage.

## Changes

- Added `MainActivity.java` native back dispatch through `OnBackPressedCallback`.
- Native back now evaluates `window.solarRemoteHandleNativeBack()` in the WebView.
- JS returns `handled` from the control page and renders the homepage; JS returns `exit` from the homepage.
- Added testable helpers for native back action, Live Status model, and nearby-device metrics.
- Reworked the control-page status card into a `LIVE STATUS` layout.
- Added 2x2 metrics inside nearby-device cards.
- Missing protocol fields such as battery percentage, morning time, and lights-off time now display `-` instead of fake values.
- Ran `npm.cmd run sync` so Android assets include the latest frontend build.

## Verification

- TDD red check: `npm.cmd test -- src/app.test.ts` failed before implementation because the new functions did not exist.
- `npm.cmd test -- src/app.test.ts`: passed 1 file / 17 tests.
- `npm.cmd test`: passed 5 files / 38 tests.
- `npm.cmd run build`: passed.
- `npm.cmd run sync`: passed.
- With temporary `JAVA_HOME=C:\Program Files\Android\Android Studio\jbr`, `gradlew.bat :app:compileDebugJavaWithJavac`: passed.
- With the same temporary JBR, `gradlew.bat :app:assembleDebug`: passed.
- Chrome 430x932 homepage screenshot captured: `.agent/reports/screenshots/2026-04-28-t018-home.png`.

## Notes

- BLE bridge, protocol code, and `deviceController.ts` were not modified.
- `@capacitor/app` was not added.
- Final acceptance still requires real-phone gesture back verification.
