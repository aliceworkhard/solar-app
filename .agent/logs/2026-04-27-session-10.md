# Session 10 - T-012 read status readable values

## Goal

执行已批准的 T-012：把读状态回传中的电池单位从 `%` 修正为 `V`，并将 `E1` 回传有效值转成 UI 可读状态。

## Changes

- 新增审批记录：`.agent/approvals/2026-04-27-read-status-readable-values.md`。
- 扩展 `DeviceStatus`，增加 `batteryVoltage`、`workMinutes`、`loadCurrentAmp`、`solarVoltage`、`statusExtraRaw`。
- `responseParser` 对 `E1` 回传增加短包保护，并结构化解析工作时长、亮度、电池电压、电池电流、太阳能电压和扩展原始值。
- App 首页和控制页增加工作时长、负载电流、太阳能电压展示；电池显示改为 `V`。
- 补充解析器测试和 UI 格式化测试。

## Verification

- `npm.cmd test -- src/protocol/responseParser.test.ts src/app.test.ts`：先按 TDD 观察到 3 个期望失败，再实现后通过。
- `npm.cmd test`：5 个测试文件、27 个测试通过。
- `npm.cmd run build`：TypeScript 与 Vite build 通过。
- `npm.cmd run sync`：build 后已同步 Web 产物到 Android 工程资源。

## Notes

- 未修改读状态命令 HEX。
- 未修改 BLE 写入/notify 机制。
- 未修改 Excel 源文件。
- `项目文件/android-mvp-capacitor/android/.idea/` 未纳入。
