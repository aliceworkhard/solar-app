# Session Log - 2026-04-28 - T-026 Profile Density And Safe Bottom Navigation

## Goal

Fix the T-025 real-phone UI feedback:

- Keep `我的设备` stats truly side-by-side.
- Keep `我的场景` truly side-by-side.
- Make the bottom tab bar smaller and safer around Android system navigation.
- Reduce the size of the newly added profile/settings content.

## Approval

- Plan: `.agent/plans/2026-04-28-t026-profile-density-safe-nav.md`
- Approval: `.agent/approvals/2026-04-28-t026-profile-density-safe-nav.md`
- User confirmation: “可以，试一下”

## Changes

- `src/app.ts`
  - Added compact profile labels: `在线 / 可连 / 离线`.
  - Added compact scene labels: `夜间 / 日常 / 节能 / 高亮`.
  - Rendered profile stats and scene labels from `PROFILE_PAGE_COPY`.
- `src/styles.css`
  - Removed the narrow-screen profile collapse that caused stats to become one column and scenes to become 2x2.
  - Kept profile stats as 3 columns and scene items as 4 columns at 320/360/390px.
  - Reduced profile card gaps, icon sizes, text sizes, list row heights, and logout button height.
  - Reduced bottom nav height to an iOS-like compact tab bar and introduced `--safe-bottom` / `--bottom-nav-space`.
  - Reduced profile/scene top header spacing while keeping a conservative top safe area.
- `src/app.test.ts`
  - Added a regression test for compact profile labels suitable for narrow Android screens.
- `.agent/reports/2026-04-28-t026-layout-check.cjs`
  - Added a local Playwright/Chrome layout check script.
- APK delivery
  - `交付物/solar-remote-t026-sideload.apk`
  - `C:\solar-apk\solar-remote-t026-sideload.apk`

## Verification

- TDD red check:
  - `npm.cmd test -- src/app.test.ts` failed as expected before implementation because `PROFILE_PAGE_COPY.deviceStatLabels` was missing.
- Green/full checks:
  - `npm.cmd test -- src/app.test.ts`: 27 tests passed.
  - `npm.cmd test`: 5 files / 48 tests passed.
  - `npm.cmd run build`: passed.
  - `npm.cmd run sync`: passed.
  - Browser layout check with system Chrome:
    - 320px: profile/device/scene `scrollWidth=320`; profile stats rows = 1; scene rows = 1; nav height = 57px.
    - 360px: profile/device/scene `scrollWidth=360`; profile stats rows = 1; scene rows = 1; nav height = 57px.
    - 390px: profile/device/scene `scrollWidth=390`; profile stats rows = 1; scene rows = 1; nav height = 57px.
  - JBR Gradle:
    - `.\gradlew.bat :app:assembleDebug --project-prop android.injected.testOnly=false`: BUILD SUCCESSFUL.
  - APK checks:
    - SHA256: `901050A738DDF4C163439DEB550A8AEF7C799FBF6EA9483840F6F3E95B3EFDD0`
    - `aapt` manifest check: `NO_TEST_ONLY`.
    - `apksigner verify --verbose`: verifies with v2 signature, 1 signer.

## Notes

- BLE native behavior, protocol HEX, normal-command response waiting, and `deviceController.ts` were not changed.
- Python Playwright was unavailable in the bundled runtime, so the layout check uses the bundled Node.js runtime, bundled Playwright package, and system Chrome executable.
- Final status still needs real-phone visual confirmation because Android/vivo safe-area behavior can differ from desktop Chrome.
