# Session Log - 2026-04-29 - T-029 Vivo Capacitor Edge-To-Edge Hardening

## Scope

- Implemented approved plan `.agent/plans/2026-04-29-t029-vivo-capacitor-edge-hardening.md`.
- Approval recorded in `.agent/approvals/2026-04-29-t029-vivo-capacitor-edge-hardening.md`.
- Input basis: user-provided `docs/try.md`.
- Did not modify BLE command semantics, protocol HEX, normal-command response waiting, or `src/device/deviceController.ts`.

## Changes

- `MainActivity.java`
  - Keeps `WindowCompat.enableEdgeToEdge(window)`.
  - Applies non-black backgrounds to window/decor/content and keeps WebView transparent.
  - Adds top and bottom native paint fallback views for system-bar areas.
  - Collects `systemBars/displayCutout/systemGestures/tappableElement` insets from DecorView and returns the original insets, not `CONSUMED`.
  - Converts native physical px to CSS px before Web injection.
  - Injects `--native-inset-*` plus legacy `--system-*`, stores `window.__nativeSystemInsets`, and re-requests insets on resume.
- `styles.xml`
  - Adds `android:windowBackground` and `android:colorBackground` fallbacks while preserving transparent system bars and light icons.
- `capacitor.config.ts`
  - Adds bundled `SystemBars` CSS handling with light style.
- `src/app.ts`
  - Allows string or numeric system inset values.
  - Writes `--native-inset-*` and legacy `--system-*`.
  - Replays `window.__nativeSystemInsets` after the app registers the bridge.
- `src/styles.css`
  - Adds `--native-inset-*`, `--safe-area-inset-*`, and `--edge-*`.
  - Uses `--edge-*` for header, scroll margin, content bottom spacing, and bottom navigation.
  - Keeps bottom nav fixed to viewport bottom while padding tab content above the bottom inset.
- `src/app.test.ts`
  - Added T-029 regression coverage for CSS-pixel inset strings.
- `.agent/reports/2026-04-29-t029-edge-hardening-layout-check.cjs`
  - Adds local Chrome/CDP checks for 320/360/390px edge layout.

## Verification

- TDD red check:
  - `npm.cmd test -- src/app.test.ts` failed first as expected because CSS px strings were normalized to `0px` and no `--native-inset-*` variables were written.
- After implementation:
  - `npm.cmd test -- src/app.test.ts`: 30 tests passed.
  - `npm.cmd test`: 5 files / 51 tests passed.
  - `npm.cmd run build`: passed.
  - `npm.cmd run sync`: passed.
  - First Gradle run exposed `onResume()` access mismatch; fixed from `protected` to `public`.
  - `$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'; .\gradlew.bat :app:assembleDebug --project-prop android.injected.testOnly=false`: BUILD SUCCESSFUL.
  - Static checks found no `SYSTEM_UI_FLAG_*`, no legacy layout fullscreen flags, no `WindowInsetsCompat.CONSUMED`, no `setStatusBarColor/setNavigationBarColor`, no `fitsSystemWindows`, and no legacy StatusBar overlay config.
  - T-029 Chrome/CDP layout check passed at 320/360/390px:
    - `scrollWidth` equals viewport width.
    - `--native-inset-*` and `--system-*` accept CSS px strings.
    - header content stays below top inset.
    - bottom nav reaches viewport bottom and active tab content stays above bottom inset.
    - html/body/#app/.shell/bottom-nav backgrounds are non-transparent.
- APK delivery:
  - `C:\solar-apk\solar-remote-t029-sideload.apk`
  - `交付物/solar-remote-t029-sideload.apk`
  - SHA256: `B80987993290FCB80D267394E6F0DEF57B2DE72C0CAD79AB5E53AD2FFB69415C`
  - `aapt dump xmltree ... AndroidManifest.xml | Select-String testOnly`: no output.
  - `apksigner verify --verbose`: verifies using APK Signature Scheme v2.

## Reports

- `.agent/reports/2026-04-29-t029-edge-hardening-layout-check.cjs`
- `.agent/reports/2026-04-29-t029-edge-hardening-layout-results.json`
- `.agent/reports/screenshots/2026-04-29-t029-edge-hardening-390x900.png`

## Remaining Risk

- Local Chrome checks cannot fully reproduce vivo/Android WebView/system-bar compositor behavior.
- Requires real-device validation for:
  - vivo / Android 16 gesture navigation.
  - vivo three-button navigation.
  - Android 13/14 gesture navigation.
  - Android 15/16 targetSdk 35/36 edge-to-edge behavior.
- If T-029 still shows black bars, next step should be a separate T-029D diagnostic color APK plan rather than another blind UI/system-bar change.
